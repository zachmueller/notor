/**
 * Orchestration domain types — skeleton (design Phase 1).
 *
 * This module is intentionally empty in design Phase 0. It is scaffolded here
 * (per ENV-001) so the run-loop substrate and Phase-1 engine can land in
 * separate commits without file-creation conflicts. The orchestration domain
 * types (`OrchestrationFlow`, `StepDefinition`, `OrchestrationEvent`, the
 * terminal-event constants, `CodeStepResult`, `OrchestrationHelper`, …) are
 * populated by FEAT-001 in Phase 1.
 *
 * Run-loop substrate types (`RunContext`, `RunResult`, `RunLoopOptions`,
 * `RunLoopHooks`, `OrchestrationToolContext`) live in `src/run-loop/types.ts`,
 * NOT here.
 *
 * @see specs/ZZ-misc/orchestration/data-model.md — Orchestration Domain Types
 * @see specs/ZZ-misc/orchestration/tasks/phase-1-engine.md — FEAT-001
 */

export {};
